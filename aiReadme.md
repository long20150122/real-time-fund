# AI 项目文档 (aiReadme.md)

## 1. 项目概述

| 属性 | 值 |
|------|-----|
| **项目名称** | real-time-fund (研估宝) |
| **版本** | 0.1.5 |
| **类型** | 纯前端 Web 应用 |
| **开源协议** | GNU Affero General Public License v3.0 (AGPL-3.0) |

### 1.1 项目简介

一个基于 Next.js 开发的基金估值与重仓股实时追踪工具。采用玻璃拟态设计（Glassmorphism），支持移动端适配。通过 JSONP 方案直连东方财富、腾讯财经等公开接口，实现纯前端运行，无需后端服务器。

---

## 2. 项目结构

```
real-time-fund/
├── app/                          # Next.js App Router 主目录
│   ├── page.jsx                  # 主页面组件 (~216KB, 核心业务逻辑)
│   ├── layout.jsx                # 根布局组件
│   ├── globals.css               # 全局样式 (玻璃拟态设计)
│   ├── icon.svg                  # 应用图标
│   ├── api/
│   │   └── fund.js               # 基金数据 API 接口 (~15KB)
│   ├── components/
│   │   ├── AnalyticsGate.jsx     # 数据分析入口组件
│   │   ├── Announcement.jsx      # 公告组件
│   │   ├── Common.jsx            # 通用组件 (~10KB)
│   │   └── Icons.jsx             # 图标组件 (~10KB)
│   ├── lib/
│   │   └── supabase.js           # Supabase 客户端配置
│   └── assets/                   # 静态资源
│       ├── github.svg
│       ├── weChatGroup.png
│       ├── weixin.jpg
│       └── zhifubao.jpg
├── doc/
│   └── webchatGroup.jpg          # 文档图片
├── supabase.sql                  # 数据库建表语句
├── docker-compose.yml            # Docker Compose 配置
├── Dockerfile                    # Docker 构建文件
├── next.config.js                # Next.js 配置
├── vercel.json                   # Vercel 部署配置
├── env.example                   # 环境变量示例
├── package.json                  # 项目依赖配置
└── README.md                     # 项目说明文档
```

---

## 3. 技术栈

### 3.1 核心框架

| 技术 | 版本 | 用途 |
|------|------|------|
| Next.js | ^16.1.5 | React 全栈框架 (App Router) |
| React | 18.3.1 | UI 组件库 |
| React DOM | 18.3.1 | React DOM 渲染 |

### 3.2 核心依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| @supabase/supabase-js | ^2.78.0 | 后端服务 (认证、数据库) |
| framer-motion | ^12.29.2 | 动画库 |
| dayjs | ^1.11.19 | 日期处理 |
| @dicebear/core | ^9.3.1 | 头像生成核心 |
| @dicebear/collection | ^9.3.1 | 头像样式集合 |

### 3.3 开发工具

| 工具 | 版本 | 用途 |
|------|------|------|
| babel-plugin-react-compiler | ^1.0.0 | React 编译器优化 |

### 3.4 运行环境要求

| 环境 | 要求 |
|------|------|
| Node.js | >= 20.9.0 |
| Docker | Node 22-bullseye 镜像 |

---

## 4. 业务功能

### 4.1 核心功能

| 功能模块 | 描述 |
|----------|------|
| **实时估值** | 输入基金代码，实时获取单位净值、估值净值及涨跌幅 |
| **重仓追踪** | 自动获取基金前 10 大重仓股票，实时追踪盘中涨跌 |
| **自选管理** | 支持添加/删除基金，Tab 切换全部/自选基金列表 |
| **自动刷新** | 可设置刷新间隔 (5秒-300秒)，支持手动刷新 |
| **数据持久化** | localStorage 本地存储，支持云端同步 (Supabase) |

### 4.2 用户系统

| 功能 | 描述 |
|------|------|
| **邮箱注册/登录** | 基于 Supabase Auth 的用户认证 |
| **配置云同步** | 登录用户可同步基金配置到云端 |
| **反馈功能** | 通过 Web3Forms 提交用户反馈 |

### 4.3 数据源

| 数据类型 | 数据源 | 技术方案 |
|----------|--------|----------|
| 基金估值 | 天天基金 | JSONP |
| 重仓数据 | 东方财富 | HTML Parsing |
| 股票行情 | 腾讯财经 | Script Tag Injection |

---

## 5. 数据库设计

### 5.1 user_configs 表

用户配置存储表，用于云端同步基金列表。

```sql
create table public.user_configs (
  id           bigint generated by default as identity not null,
  created_at   timestamp with time zone not null default now(),
  data         json null,                    -- 用户配置数据 (JSON)
  updated_at   text null,                    -- 更新时间
  user_id      uuid not null,                -- 用户ID (关联 auth.users)
  constraint user_configs_pkey primary key (id),
  constraint user_configs_user_id_key unique (user_id)
);
```

### 5.2 行级安全策略 (RLS)

| 策略名 | 操作 | 规则 |
|--------|------|------|
| user_configs_select_own | SELECT | auth.uid() = user_id |
| user_configs_insert_own | INSERT | auth.uid() = user_id |
| user_configs_update_own | UPDATE | auth.uid() = user_id |

---

## 6. 环境配置

### 6.1 环境变量

| 变量名 | 必填 | 用途 |
|--------|------|------|
| NEXT_PUBLIC_SUPABASE_URL | 否 | Supabase 项目 URL |
| NEXT_PUBLIC_SUPABASE_ANON_KEY | 否 | Supabase 匿名公钥 |
| NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY | 否 | Web3Forms 反馈表单密钥 |

> 注：不使用登录和反馈功能时，环境变量可不配置。

### 6.2 Supabase 配置

1. **获取密钥路径**：
   - URL: Supabase 控制台 → Project Settings → General → Project ID
   - ANON_KEY: Supabase 控制台 → Project Settings → API Keys → Publishable key

2. **邮件配置**：
   - 免费额度：每小时 2 条邮件
   - 自定义 SMTP: 控制台 → Authentication → Email → SMTP Settings
   - 验证码格式: 控制台 → Authentication → Email → Confirm sign up → 选择 `{{.token}}`

---

## 7. 部署方式

### 7.1 本地开发

```bash
# 安装依赖
npm install

# 配置环境变量
cp env.example .env.local

# 启动开发服务器
npm run dev
```

### 7.2 静态导出

```bash
npm run build
# 输出目录: out/
```

### 7.3 Docker 部署

```bash
# 构建镜像
docker build -t real-time-fund .

# 运行容器
docker run -d -p 3000:3000 --name fund real-time-fund

# 或使用 docker-compose
docker compose up -d
```

### 7.4 GitHub Pages 部署

项目已配置 GitHub Actions，推送到 `main` 分支自动构建部署。

需在 GitHub Settings → Secrets and variables → Actions 配置：
- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY
- NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY

---

## 8. Next.js 配置

```javascript
const nextConfig = {
  reactStrictMode: true,    // 启用 React 严格模式
  reactCompiler: true,      // 启用 React 编译器优化
};
```

---

## 9. 前端设计

### 9.1 设计风格

- **玻璃拟态 (Glassmorphism)**: 毛玻璃效果、半透明背景
- **响应式设计**: 完美适配 PC 与移动端
- **动画交互**: 基于 Framer Motion 的流畅动画

### 9.2 样式方案

- 原生 CSS (Global CSS)
- CSS Variables 主题配置
- 移动端优先的媒体查询

---

## 10. API 接口

### 10.1 基金数据接口 (app/api/fund.js)

| 接口 | 描述 |
|------|------|
| 基金基本信息 | 获取基金名称、净值等 |
| 实时估值 | 获取盘中实时估值 |
| 重仓股票 | 获取前 10 大重仓股信息 |
| 股票行情 | 获取重仓股实时涨跌 |

---

## 11. 关键文件说明

| 文件 | 职责 |
|------|------|
| `app/page.jsx` | 主页面，包含所有核心业务逻辑和 UI 组件 |
| `app/api/fund.js` | 封装基金/股票数据获取的 JSONP 接口 |
| `app/lib/supabase.js` | Supabase 客户端初始化 |
| `app/components/Common.jsx` | 通用 UI 组件 (卡片、按钮等) |
| `app/globals.css` | 全局样式，包含玻璃拟态设计样式 |

---

## 12. 注意事项

1. **跨域处理**: 采用 JSONP 和 Script Tag Injection 方案，无需代理服务器
2. **数据准确性**: 数据来自公开接口，可能存在延迟，仅供学习参考
3. **开源协议**: AGPL-3.0，通过网络服务提供修改版本时需开源
